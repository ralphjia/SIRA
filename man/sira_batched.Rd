% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sira_batched.R
\name{sira_batched}
\alias{sira_batched}
\title{Scalable Image-on-Scalar Regression Algorithm (SIRA) â€” Batched variant}
\usage{
sira_batched(
  Y_files,
  X,
  Z,
  d1 = NULL,
  d2 = NULL,
  d3 = NULL,
  lambda,
  mu,
  coords = NULL,
  Y_reader = readRDS,
  Psi_star = NULL,
  max_iter = 50L,
  delta = 0.01,
  inner_loss_tol = NULL,
  verbose = TRUE
)
}
\arguments{
\item{Y_files}{Character vector of file paths. Each file must be readable by
\code{Y_reader} and return a numeric matrix with \eqn{V} columns.}

\item{X}{An \eqn{n \times p_1} numeric matrix of covariates of interest.}

\item{Z}{An \eqn{n \times p_2} numeric matrix of confounders. Should
typically include a column of ones for the intercept.}

\item{d1}{Integer. Number of voxels along image axis 1.}

\item{d2}{Integer. Number of voxels along image axis 2.}

\item{d3}{Integer. Number of voxels along image axis 3. Use \code{1} for
2D images.}

\item{lambda}{Non-negative numeric. Spatial penalty controlling boundary
smoothness. Larger values produce larger, blockier regions.}

\item{mu}{Non-negative numeric. L1 sparsity penalty. Larger values produce
sparser solutions (fewer nonzero voxels).}

\item{coords}{Optional \eqn{V \times 3} numeric matrix of voxel coordinates
for irregular or masked layouts. Supply \code{coords} instead of
\code{d1}, \code{d2}, \code{d3} when voxels do not lie on a full
rectangular grid.}

\item{Y_reader}{Function with signature \code{function(path) -> matrix}.
Default is \code{readRDS}. Use a custom reader for CSV, HDF5, etc.}

\item{Psi_star}{Optional \eqn{L \times V} numeric matrix. Spatial individual
effects basis computed by \code{BayesGPfit}. If \code{NULL} (default),
SIRA calls \code{BayesGPfit} internally with \code{poly_degree = 8}.}

\item{max_iter}{Integer. Maximum outer coordinate-descent iterations.
Default \code{50}.}

\item{delta}{Numeric. Convergence threshold on the Frobenius norm of the
change in \eqn{\hat\alpha} between iterations. Default \code{0.01}.}

\item{inner_loss_tol}{Optional non-negative numeric. Minimum loss decrease
required to accept a region operation within a covariate update. If
\code{NULL}, defaults to \code{1e-7}.}

\item{verbose}{Controls progress output. Use \code{FALSE} for silent mode,
\code{TRUE} for iteration-level progress, or \code{"ops"} for detailed
region-operation logging.}
}
\value{
An object of class \code{"sira"}.
}
\description{
Fits the same model as \code{\link{sira}} but reads the outcome matrix
\eqn{Y} from disk in batches, so the full \eqn{n \times V} matrix is
never held in memory simultaneously. For repeated fits across many
\code{(lambda, mu)} combinations, prefer
\code{\link{sira_batched_preprocess}} followed by
\code{\link{sira_batched_fit}}.
}
\examples{
\dontrun{
fit <- sira_batched(
  Y_files = Y_files,
  X = X, Z = Z,
  d1 = d1, d2 = d2, d3 = d3,
  lambda = 0.3, mu = 0.1
)
}

}
\seealso{
\code{\link{sira}}, \code{\link{sira_batched_preprocess}},
  \code{\link{sira_batched_fit}}
}
